<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>

    <!-- google fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto&family=Roboto+Condensed:wght@100..900&display=swap" rel="stylesheet">

    <!-- make sure the styles are in /public/styles folder -->
    <link href="/styles/root.css" rel="stylesheet">
    <% if (locals.styles) { %> 
      <link href="/styles/<%= styles %>.css" rel="stylesheet">
    <% }; %>


  </head>
  <body>
    <header>
      <h2 class="title">
        <a href="/">Clubhouse</a>
      </h2>
      <%- include("./partials/profileNotificationIcon.ejs") %>
    </header>
    <%- include("./partials/navbar.ejs") %>
      <!-- looks for locals.mainView, e.g. locals.mainView = "homepage" -->    
      <!-- locals.mainView should be the <main> of a page  -->
      <!-- and must live directly inside the 'views' folder -->
      <%- include(`./${mainView}.ejs`) %>
    <footer>
      <p>Build by Snehamoy Bag</p>
    </footer>
  </body>
</html>
